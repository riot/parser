/**
 * Not all the types are handled in this module.
 *
 * @enum {number}
 * @readonly
 */
export default {
  TAG: NodeTypes.TAG,
  ATTR: NodeTypes.ATTR,
  TEXT: NodeTypes.TEXT,
  CDATA: NodeTypes.CDATA,
  COMMENT: NodeTypes.COMMENT,
  DOCUMENT: NodeTypes.DOCUMENT,
  DOCTYPE: NodeTypes.DOCTYPE,
  DOCUMENT_FRAGMENT: NodeTypes.DOCUMENT_FRAGMENT,
}
