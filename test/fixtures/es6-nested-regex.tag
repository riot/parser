<es6-nested-regex>
  <h1>{ message }</h1>

  <script>
    const x = 1

    export default {
      message: `
        regex: ${
          /x}`/.source
        }`,

      foobar() { `
        foobar() {
          return ${
            foo( 5 /x/.1 )  // not regex
          }
        }`
      }
    }





  function foo(bar) {

  }

  function foo(bar) {

  }

  </script>

  <style>
    :root {
      color: blue;
    }
  </style>
</es6-nested-regex>
